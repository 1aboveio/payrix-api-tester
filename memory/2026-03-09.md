- Issue #60 invoice Phase 6 stabilization: added stable merchant select hooks on `/platform/invoices/create`, updated invoice E2E selectors to use them, and replaced unsupported Payrix customer/invoice search filters in the real-API spec with list-and-find lookups.
- Static verification passed: `npx tsc --noEmit` and `npx eslint e2e/platform-endpoints.spec.ts e2e/platform-real-api.spec.ts src/app/platform/invoices/create/page.tsx`.
- Full Playwright E2E execution is blocked in this sandbox because Next.js cannot bind any local port (`listen EPERM` on `127.0.0.1:3000`). Moved stale `test-results/` out of the repo to `/tmp/payrix-api-tester-test-results-20260309`.
