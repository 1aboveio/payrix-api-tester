# 2026-02-18

## Session notes

- Researched `office-openclaw` repo — team shared memory for 1Above. 19 skills, full team roster documented in TEAM.md.
- Created `playwright-browserless` skill with Docker setup, Playwright connection patterns (TS + Python), and common patterns in `references/patterns.md`.
- Added anti-patterns section: no Puppeteer/Selenium, always close browser/page in finally blocks, no shared browser instance, no fixed sleeps, no unclosed pages.
- Jonas set operating guidelines: act with discretion on small/confident tasks; propose plan for uncertain/complex ones; always ask before dangerous actions (delete, merge to main, uninstall, server restart).

---

## Evening session — cover-gen E2E loop (2026-02-18 ~20:58 HKT)

### Project
- Repo: `/tmp/cover-gen-1above` (branch: `dev`)
- GCP: `cosmic-heaven-479306-v5` (number `903828198190`)
- Cloud Run URL: `https://cover-gen-dev-903828198190.us-central1.run.app`
- IAP OAuth client: `369001918367-t5qrahnqdaasaifvk6akpqkpjk9vli58.apps.googleusercontent.com`

### Key decisions made
- GCS images served via `/api/image?path=` proxy (org-level public access prevention blocks allUsers)
- `force-dynamic` on `app/page.tsx` — prevents build-time empty-bucket snapshot for library
- Hydration: localStorage reads deferred to `useEffect` after mount (never in SSR render)
- Delete flow: removed `window.confirm()` from Sidebar.tsx — Playwright/Browserless can't intercept native dialogs
- Cloud Build trigger `cover-gen-dev` lives in `us-central1` — always `gcloud config set builds/region us-central1`
- Playwright `retries: 1` added for browser-drop resilience (J4/J6)
- Delete button: must remove `md:opacity-0 md:group-hover:opacity-100` from className — headless CDP can't hover

### Infra notes
- Browserless: `ws://localhost:3000`, globalSetup restarts it before each E2E run
- E2E run command: `cd /tmp/cover-gen-1above && GEMINI_API_KEY=$(grep GEMINI_API_KEY .env.local | cut -d= -f2) GCS_BUCKET_NAME=$(grep GCS_BUCKET_NAME .env.local | cut -d= -f2) GOOGLE_APPLICATION_CREDENTIALS=/home/exoulster/.config/gcloud/application_default_credentials.json USE_CLOUD_RUN=true npx playwright test --reporter=list --timeout=120000 2>&1`
- Cron job `e2e-loop-monitor` id `d602413e-3666-42ba-a612-e99ab4228f8d`, every 5 min, posts to `channel:1472870267087884339`
- Latest deployed commit: `ec915b0`

### Status at compaction
- 7 failures from e2e-loop-3 identified; fixes in progress:
  1. Sidebar.tsx line 464: remove `md:opacity-0 md:group-hover:opacity-100` from delete button
  2. Add `retries: 1` to Playwright config
  3. Fix J8 mobile backdrop — use Escape key / close button fallback
- Next: commit + push, wait for Cloud Build SUCCESS, spawn e2e-loop-4

---

## Late evening — skills & gateway config (2026-02-18 ~21:32 HKT)

### Skills created/updated in office-openclaw repo
- `playwright-browserless/SKILL.md` — Podman (not Docker), `--shm-size=8gb`, `-e "CONNECTION_TIMEOUT=300000"`, container management section, anti-patterns
- `playwright-browserless/references/patterns.md` — scraping, screenshots, PDF, forms, concurrency, Podman Compose
- `cicd-pipeline/SKILL.md` — state machine (IDLE→BUILDING_LOCAL→PUSHING→CI_BUILDING→E2E_RUNNING→E2E_FIXING→DONE), Step 0 clarification, slash commands `/cicd_pipeline start|stop|status`
- `cicd-pipeline/references/step-patterns.md` — per-step patterns + anti-patterns
- `cicd-pipeline/references/definitions.md` — definition of success, stop conditions, escalation format

### Key cicd-pipeline decisions
- Local build: no tmux (synchronous, inline)
- Push: dev + feature branches allowed; hard stop on `main`
- buildId: captured after push via `--filter="substitutions.BRANCH_NAME=<branch>"`
- e2eMode: clarified in Step 0 (local vs cloud-run-job)
- E2E_FIXING: agent picks tool freely (direct edit, Claude Code via tmux, exec)
- Slash commands auto-registered via `commands.nativeSkills: "auto"` → `cicd_pipeline` (sanitized)

### Gateway config changes
- Discord: `blockStreaming: true`, `blockStreamingDefault: "on"`, `blockStreamingBreak: "text_end"`, `blockStreamingChunk: { minChars: 800, maxChars: 2000, breakPreference: "paragraph" }`, `blockStreamingCoalesce: { idleMs: 1000 }`
- Brave Search API key configured: `tools.web.search.apiKey`
- xAI API key: provided key `SKgYJVSBTWn4czQXRfqx6...` returned 401 (wrong format, expected `xai-` prefix) — NOT stored

### All commits use Grace identity
- `git config user.name "Grace" && git config user.email "grace@1above.io"`
- Remote: `git@github.com:1aboveio/office-openclaw.git` via `github-grace` SSH alias
